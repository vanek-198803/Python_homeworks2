# Дефекты в классе StringUtils:

1. Метод delete_symbol не учитывает ситуацию, когда symbol - это строка длиной более 1 символа.
   Пример: delete_symbol("SkyPro", "Pro") возвращает "Sky", но не срабатывает на отдельные символы или подстроки.

2. Метод list_to_string может возвращать пустую строку вместо ожидаемого значения с разделителем, если список содержит один элемент.
   Например, list_to_string(["Sky"]) должен возвращать "Sky", а не пустую строку.pytest